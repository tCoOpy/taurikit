# {{APP_NAME}} - Cursor Rules

Generated by Crabyard. Use these rules when turning this scaffold into a real desktop app.

Keep this file in sync with `AGENTS.md`, `CLAUDE.md`, and `.github/copilot-instructions.md`.

## Project Context

- App name: `{{APP_NAME}}`
- Auth module: `{{AUTH_MODULE}}`
- UI module: `{{UI_MODULE}}`
- Package manager: `{{PACKAGE_MANAGER}}`
- Frontend: React 19, TypeScript, Vite, Tailwind CSS v4
- Desktop backend: Tauri v2, Rust, async IPC commands
- State: Zustand in `src/store/appStore.ts`
- Frontend/backend bridge: typed wrappers in `src/lib/tauri.ts`

The frontend runs in a desktop webview. The Rust backend owns native capabilities such as filesystem access, OS APIs, HTTP, menus, windows, persistence, auth, and updates. Frontend code calls backend behavior through Tauri `invoke`, wrapped in `src/lib/tauri.ts`.

## Main Files

- `src/App.tsx`: root shell after auth/UI overlays are applied.
- `src/components/DashboardView.tsx`: default app workspace; start here when replacing the scaffold UI.
- `src/components/TitleBar.tsx`: custom window chrome for macOS, Windows, and Linux.
- `src/components/ui/`: reusable UI primitives supplied by the selected UI overlay.
- `src/components/UpdateBanner.tsx`: update notification and install flow.
- `src/components/OnboardingTour.tsx`: scaffold onboarding; remove or replace once the real product flow exists.
- `src/hooks/useSettings.ts`: settings load/save and folder picker.
- `src/hooks/useTheme.ts`: theme synchronization.
- `src/hooks/useUpdater.ts`: app update checks and install behavior.
- `src/hooks/useAuth.ts`: present when the selected auth module needs login state.
- `src/store/appStore.ts`: shared frontend state.
- `src/lib/tauri.ts`: the only place frontend code should call `invoke`.
- `src/lib/types.ts`: TypeScript models that mirror Rust models.
- `src-tauri/src/lib.rs`: Tauri builder, plugins, menus, state, and command registration.
- `src-tauri/src/commands/`: Rust IPC command handlers.
- `src-tauri/src/models/`: Rust serde models shared with TypeScript.
- `src-tauri/src/state.rs`: shared backend state.
- `src-tauri/src/error.rs`: `AppError` and `AppResult<T>`.
- `src-tauri/capabilities/default.json`: Tauri plugin permission grants.

## Working Rules

- Read the relevant files before editing. Do not infer architecture from a generic React or Tauri template.
- Build the requested product workflow directly. Replace scaffold demo content instead of adding a marketing page in front of it.
- Keep changes scoped to the requested feature. Avoid unrelated rewrites, dependency swaps, or formatting churn.
- Preserve the desktop shell unless asked: title bar, app menu, update banner, settings/theme handling, and error boundary.
- Prefer existing primitives, hooks, state, and wrappers before adding new abstractions.
- Keep TypeScript strict. Do not use `any` to bypass model design.
- Add comments only when a reviewer would ask why the code exists. Do not narrate standard syntax or obvious control flow.

## Frontend Patterns

- Use React function components and hooks.
- Use the `@/` import alias for project imports.
- Use UI primitives from `src/components/ui/` before creating new primitives.
- Treat `{{UI_MODULE}}` as the selected UI system. Do not assume shadcn-only APIs unless the generated project actually has them.
- Use Tailwind CSS v4 utilities and theme variables from `src/index.css`.
- Use `cn()` from `src/lib/utils.ts` for conditional class composition.
- Put shared app state in Zustand only when multiple screens/components need it. Keep local UI state local.
- For user feedback, use existing UI primitives and `sonner` where toast feedback is appropriate.
- Make desktop workflows feel native: stable layouts, clear loading/error states, keyboard-friendly controls, and no mobile-only assumptions.
- Do not call browser window APIs such as `window.close()` or `window.minimize()`. Use Tauri window APIs.
- Do not use `fetch()` to call local backend behavior. Use typed Tauri command wrappers.

## Backend And IPC Patterns

- All Rust commands return `AppResult<T>`.
- Register every command in `src-tauri/src/lib.rs` with `tauri::generate_handler!`.
- Add every command to `CommandMap` in `src/lib/tauri.ts`.
- Export a typed wrapper from `src/lib/tauri.ts`; React components should import wrappers, not `invoke`.
- Use `#[serde(rename_all = "camelCase")]` on Rust models shared with TypeScript.
- Mirror shared Rust models in `src/lib/types.ts` using camelCase field names.
- Keep `Mutex<AppState>` lock scopes short. Never hold the lock across slow I/O or network work.
- Map backend failures into `AppError` instead of returning raw strings or panicking.
- Update `src-tauri/capabilities/default.json` when a new Tauri plugin requires permissions.

## Add A Tauri Command

1. Create or extend a Rust command module in `src-tauri/src/commands/`.

```rust
use crate::error::{AppError, AppResult};
use crate::state::SharedState;
use tauri::State;

#[tauri::command]
pub async fn do_something(
    state: State<'_, SharedState>,
    input: String,
) -> AppResult<String> {
    let _state = state
        .lock()
        .map_err(|err| AppError::Other(err.to_string()))?;

    Ok(format!("Result: {input}"))
}
```

2. Export the module from `src-tauri/src/commands/mod.rs`.

```rust
pub mod myfeature;
```

3. Register the command in `src-tauri/src/lib.rs`.

```rust
tauri::generate_handler![
    commands::myfeature::do_something,
]
```

4. Add the command contract and wrapper in `src/lib/tauri.ts`.

```typescript
type CommandMap = {
  do_something: [{ input: string }, string];
};

export async function doSomething(input: string): Promise<string> {
  return invokeCommand("do_something", { input });
}
```

5. Use the wrapper from React.

```typescript
import * as api from "@/lib/tauri";

const result = await api.doSomething("hello");
```

If the command exchanges structured data, add the Rust type in `src-tauri/src/models/`, export it from `models/mod.rs`, and mirror it in `src/lib/types.ts`.

## Feature Workflow

When asked to build a feature:

1. Identify the user-facing flow and replace scaffold placeholder UI in `DashboardView.tsx` or add a focused view component.
2. Define shared data models in Rust and TypeScript when data crosses IPC.
3. Add backend commands for native work, persistence, HTTP, or filesystem access.
4. Add typed frontend wrappers in `src/lib/tauri.ts`.
5. Wire state through local component state, hooks, or Zustand depending on scope.
6. Add loading, empty, error, and success states.
7. Run the smallest checks that cover the files changed.

## Auth, Settings, And Updates

- `{{AUTH_MODULE}}` is the selected auth overlay. If it is `none`, do not invent login state unless the user asks.
- For GitHub or Google auth, keep login/logout/restore behavior in `useAuth.ts` and the matching Rust auth commands.
- Store user preferences in `AppSettings` and update both `src-tauri/src/models/settings.rs` and `src/lib/types.ts`.
- Use `useSettings.ts` rather than duplicating settings persistence in components.
- Keep updater behavior in `useUpdater.ts` and `UpdateBanner.tsx`.

## Environment And Secrets

- `.env` is local and gitignored.
- `.env.example` contains placeholders and is safe to commit.
- `VITE_` variables are exposed to frontend code.
- Backend-only secrets must not use the `VITE_` prefix.
- Do not hardcode tokens, OAuth secrets, API keys, or filesystem-specific paths in frontend code.

## Commands

```bash
{{PM_TAURI_DEV}}
cd src-tauri && cargo check
cd src-tauri && cargo test
```

Use `{{PACKAGE_MANAGER}}` for frontend scripts:

| Package manager | Build | Test | Lint |
|-----------------|-------|------|------|
| bun | `bun run build` | `bun test` | `bun run lint` |
| pnpm | `pnpm build` | `pnpm test` | `pnpm lint` |
| yarn | `yarn build` | `yarn test` | `yarn lint` |
| npm | `npm run build` | `npm test` | `npm run lint` |

For frontend-only work, run the relevant frontend check. For backend or IPC work, run `cargo check` and any relevant Rust tests. For full desktop behavior, run the Tauri dev command.

## Hard Rules

- Do not bypass `src/lib/tauri.ts` for backend calls.
- Do not add a Rust command without a typed frontend wrapper.
- Do not skip Rust/TypeScript model synchronization for IPC payloads.
- Do not use browser APIs for native windows, dialogs, filesystem, updater, or process behavior.
- Do not put secrets in frontend code.
- Do not replace Zustand with another state library unless explicitly requested.
- Do not modify title-bar window controls unless the task is about window chrome.
- Do not leave scaffold onboarding or demo panels in place when building the real requested app flow.
