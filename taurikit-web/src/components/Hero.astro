<section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-zinc-950 noise">
  <!-- Gradient blobs -->
  <div class="absolute top-[-20%] left-[-10%] w-[600px] h-[600px] rounded-full bg-brand-600/20 blur-[128px] float-slow"></div>
  <div class="absolute bottom-[-10%] right-[-10%] w-[500px] h-[500px] rounded-full bg-brand-500/15 blur-[100px] float-slow" style="animation-delay: -3s;"></div>
  <div class="absolute top-[30%] right-[20%] w-[300px] h-[300px] rounded-full bg-brand-400/10 blur-[80px] pulse-glow"></div>

  <div class="relative z-10 max-w-5xl mx-auto px-4 sm:px-6 pt-28 sm:pt-36 pb-20 text-center flex flex-col items-center">
    <div class="mb-6 inline-flex items-center gap-2 px-4 py-1.5 rounded-full border border-white/10 bg-white/5 backdrop-blur-sm text-brand-400 text-xs sm:text-sm font-semibold tracking-wide">
      <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
      Built for Tauri v2 + React + TypeScript
    </div>

    <h1 class="text-5xl md:text-[72px] tracking-tight mb-5 leading-tight md:leading-none flex flex-col items-center gap-1 sm:gap-3">
      <span class="text-white/90 font-light">Ship desktop apps</span>
      <span class="font-extrabold flex items-center gap-3">
        <span class="text-white">in</span>
        <span id="typing-word" class="bg-gradient-to-r from-brand-400 via-brand-500 to-brand-300 bg-clip-text text-transparent inline-block min-w-[3ch] text-left"></span>
        <span class="typing-cursor"></span>
      </span>
    </h1>

    <p class="text-base sm:text-lg text-zinc-400 max-w-2xl mx-auto mb-10 leading-relaxed font-light px-2">
      A production-ready starter kit for Rust Tauri desktop apps.
      Auth, settings, UI components, auto-updates — all wired up.
      Pick your stack. Run one command. Start building.
    </p>

    <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-4">
      <a
        href="#pricing"
        class="group px-8 py-3.5 bg-gradient-to-r from-brand-600 via-brand-500 to-brand-400 text-white rounded-xl font-bold text-lg inline-flex items-center gap-3 shadow-2xl shadow-brand-500/30 hover:shadow-brand-500/50 hover:-translate-y-1 transition-all active:scale-[0.98] relative overflow-hidden"
      >
        <span class="relative z-10 flex items-center gap-3">
          Get TauriKit — $49
          <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
          </svg>
        </span>
      </a>
      <a
        href="#features"
        class="px-8 py-3.5 border border-white/10 hover:border-white/25 text-zinc-300 hover:text-white rounded-xl font-medium text-lg transition-all backdrop-blur-sm"
      >
        See what's included
      </a>
    </div>

    <p class="text-zinc-500 text-xs sm:text-sm font-medium mb-14 flex items-center gap-2">
      One-time purchase · Unlimited projects · Free updates forever
    </p>

    <!-- Glass terminal window -->
    <div class="relative max-w-3xl mx-auto w-full">
      <div class="absolute -inset-1 bg-gradient-to-r from-brand-600/20 via-brand-500/10 to-brand-400/20 rounded-2xl blur-xl"></div>
      <div class="relative glass rounded-2xl p-6 text-left font-mono text-sm shadow-2xl shadow-black/40">
        <div class="flex items-center gap-2 mb-4">
          <div class="w-3 h-3 rounded-full bg-zinc-700 hover:bg-red-500 transition"></div>
          <div class="w-3 h-3 rounded-full bg-zinc-700 hover:bg-yellow-500 transition"></div>
          <div class="w-3 h-3 rounded-full bg-zinc-700 hover:bg-green-500 transition"></div>
          <span class="ml-auto text-[10px] text-zinc-600 uppercase tracking-widest">terminal</span>
        </div>
        <div class="text-zinc-400">
          <span class="text-zinc-600">$</span>
          <span class="text-brand-400"> taurikit</span>
          <span class="text-zinc-300"> new</span>
          <span class="text-green-400"> "My App"</span>
          <span class="text-zinc-600"> --auth github --ui shadcn</span>
        </div>
        <div class="mt-3 space-y-1">
          <div class="text-zinc-600"><span class="text-green-500/80">✓</span> Copied base template</div>
          <div class="text-zinc-600"><span class="text-green-500/80">✓</span> Applied auth/github overlay</div>
          <div class="text-zinc-600"><span class="text-green-500/80">✓</span> Applied ui/shadcn overlay</div>
          <div class="text-zinc-600"><span class="text-green-500/80">✓</span> Installed dependencies</div>
          <div class="text-green-400">✓ Project ready at ./my-app</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bottom gradient fade to white -->
  <div class="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-white to-transparent"></div>
</section>

<script>
  const words = ["minutes", "seconds", "days"];
  const target = "minutes";
  let wordIndex = 0;
  let charIndex = 0;
  let isDeleting = false;
  let isPaused = false;
  const el = document.getElementById("typing-word");

  function tick() {
    if (!el) return;
    const currentWord = words[wordIndex];

    if (isPaused) {
      isPaused = false;
      isDeleting = true;
      setTimeout(tick, 100);
      return;
    }

    if (isDeleting) {
      charIndex--;
      el.textContent = currentWord.substring(0, charIndex);
      if (charIndex === 0) {
        isDeleting = false;
        wordIndex = (wordIndex + 1) % words.length;
        setTimeout(tick, 300);
        return;
      }
      setTimeout(tick, 60);
    } else {
      charIndex++;
      el.textContent = currentWord.substring(0, charIndex);
      if (charIndex === currentWord.length) {
        isPaused = true;
        setTimeout(tick, 2000);
        return;
      }
      setTimeout(tick, 120);
    }
  }

  tick();
</script>
